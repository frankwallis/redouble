/// <reference path="../../_references.d.ts" />

import {Bid} from '../bid';

describe("Bid", () => {

    beforeEach(() => {});

    describe("compare", () => {
       it("obeys order of preference of suits", () => {
       });
    });

    describe("stringify", () => {
      it("returns unique values", () => {

      });
    });
});
